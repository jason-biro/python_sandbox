# python_sandbox

## quantum_python
Using Python 3.14.6

Steps for setup:
1. In Command Prompt, run: `python -m venv .venv`
2. Then run: `source .venv/Scripts/activate`
3. Install Qiskit for the Quantum library and simulator: `pip install qiskit qiskit-aer matplotlib`
